# Pinned floors for the Dockerfile (which can't use `pip install -e .` cleanly
# inside the build cache layer). For local installs prefer `pip install -e .`
# — pyproject.toml is the source of truth.
streamlit>=1.30.0
pyexiftool>=0.5.0
Pillow>=10.0.0
anthropic>=0.18.0
mistralai>=1.0.0
geopy>=2.4.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
watchdog>=3.0.0
python-dateutil>=2.8.0
PyYAML>=6.0.0
opencv-python>=4.9.0
numpy>=1.26.0
hdbscan>=0.8.40
insightface>=0.7.3
onnxruntime>=1.17.0
# python-sane>=2.9.0  # Optional: requires SANE dev headers to build
